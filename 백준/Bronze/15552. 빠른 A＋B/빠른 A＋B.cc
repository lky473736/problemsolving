#include <bits/stdc++.h>
using namespace std;
// typedef long long ll;

int main(void) {
    ios::sync_with_stdio(false); cin.tie(0);
    
    int T; cin >> T;
    for (int i = 0; i < T; i++) {
        int a, b;
        cin >> a >> b;
        cout << a + b << '\n';
    }
    return 0;
}